spring:
  application:
    name: survey-service
  data:
    mongodb:
      database: survey-service
      host: localhost
      port: 27017
      password: bdung
      username: mongoadmin

de:
  flapdoodle:
    mongodb:

      embedded:
        version: 6.0.5

